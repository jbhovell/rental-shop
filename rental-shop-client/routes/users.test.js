var axios = require('axios');
const { describe } = require('../../rental-shop-api/models/customers');
var {fetchApi} = require('./users')

/* GET users listing. */
const url = 'http://localhost:3001/api/cost';

describe ('it fetches the api', () => {
    it ('should fetch', async () => {

    })
    it ('')
})
